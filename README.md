# 剧本创作总控台（第一阶段原型）

这是独立于“短剧生产助手”和“爽文出海创作台”的工作台原型。当前完成三栏界面、项目切换、新建项目、阶段徽标、任务线程、资料卡、版本对比、运行模拟和人工任务输入；项目和活动日志已接入本地 SQLite，可在重启后恢复。

## 本地运行

在当前目录执行：

```powershell
python server.py
```

然后访问：

```text
http://127.0.0.1:8786
```

本阶段不会调用模型、不保存API Key，也不会修改旧项目数据库。当前只写入本目录的 `workbench.sqlite3`；下一阶段再接入真实SOP审计、模型任务队列和正文版本存储。

## GitHub

本项目发布为独立私有仓库。仓库中不包含本地 SQLite 数据库、运行日志和任何密钥；这些内容只保留在本机工作目录。
