const $ = id => document.getElementById(id);
const assetDefs = {
  synopsis:{title:'剧情梗概',icon:'✦',hint:'主线、人物目标与阶段性结果'},
  characters:{title:'人物小传',icon:'◎',hint:'身份、性格、动机、弱点与成长'},
  world:{title:'世界观设定',icon:'◇',hint:'时代、空间、规则、限制与代价'},
  beat_matrix:{title:'卡点矩阵',icon:'▤',hint:'每集任务、大事件、代价与钩子'}
};
let projects=[], currentDetail=null, editingAsset=null, viewingVersion=false, viewingVersionId=null, toastTimer;
const CONFIG_KEY='scriptWorkbench.llm.session';

const escapeHtml = value => String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function showToast(message){$('toast').textContent=message;$('toast').classList.remove('hidden');clearTimeout(toastTimer);toastTimer=setTimeout(()=>$('toast').classList.add('hidden'),2400)}
async function request(url,options={}){const r=await fetch(url,{...options,headers:{'Content-Type':'application/json',...(options.headers||{})}});const data=await r.json();if(!r.ok)throw new Error(data.error||'请求失败');return data}
async function loadHealth(){try{const h=await request('/api/health');$('connectionLabel').textContent=h.storage==='cloudflare-d1'?'云端工作区已连接':'本地工作区已连接'}catch{$('connectionLabel').textContent='工作区连接异常'}}
function currentProject(){return projects.find(p=>p.selected)}
function formatTime(value){if(!value)return'—';const d=new Date(value);return Number.isNaN(d.getTime())?'—':d.toLocaleString('zh-CN',{month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit'})}
function getModelConfig(){try{return JSON.parse(sessionStorage.getItem(CONFIG_KEY)||'{}')}catch{return{}}}
function setModelConfig(value){sessionStorage.setItem(CONFIG_KEY,JSON.stringify(value));renderModelName()}
function renderModelName(){const c=getModelConfig();$('modelName').textContent=c.model||'未配置模型'}

function renderProjects(){
  $('projectList').innerHTML=projects.map((p,i)=>`<div class="project-item ${p.selected?'selected':''}" data-project="${i}">${escapeHtml(p.name)}<small>${escapeHtml(p.note)}</small></div>`).join('');
  $('projectList').querySelectorAll('.project-item').forEach(x=>x.addEventListener('click',()=>selectProject(Number(x.dataset.project))));
}
async function loadProjects(){
  try{const data=await request('/api/projects');projects=data.projects.map((p,i)=>({...p,selected:i===0}));renderProjects();if(projects.length)await selectProject(0,false)}catch(error){showToast('项目服务连接失败：'+error.message)}
}
async function selectProject(index,notify=true){
  projects.forEach((p,i)=>p.selected=i===index);renderProjects();const p=projects[index];
  try{const data=await request(`/api/projects/${encodeURIComponent(p.id)}`);currentDetail=data.detail;renderDetail();if(notify)showToast(`已切换到「${p.name}」`)}catch(error){showToast(error.message)}
}
function renderDetail(){
  const {project,profile,assets={},versions=[],gates=[],events=[],sources=[],audits=[],detectors=[],runs=[],continuity=[]}=currentDetail;
  $('projectTitle').textContent=project.name;$('breadcrumbProject').textContent=project.name;$('projectRoute').textContent=project.route;
  $('projectMeta').innerHTML=`<span class="tag">${escapeHtml(profile?.region||'待设定')}</span><span class="tag">${escapeHtml(profile?.medium||'待设定')}</span><span class="tag">${escapeHtml(profile?.genre||'待设定')}</span><span class="muted">总集数 ${profile?.total_episodes||60} · 本次提交 ${profile?.submission_start||1}—${profile?.submission_end||10} 集 · ${escapeHtml(profile?.opening_template||'未设模板')}</span>`;
  $('phaseTitle').textContent=`${profile?.current_stage||'G0 立项与参数'} · ${profile?.status||'待立项'}`;const progress=Number(profile?.progress||10);$('phaseProgress').style.width=`${progress}%`;$('progressText').textContent=`${progress}%`;
  renderSources(sources);renderAssets(assets);renderVersions(versions);renderGates(gates);renderEvents(events);renderThread(events,project);renderQuality(audits,detectors);renderRuns(runs);renderContinuity(continuity);refreshModuleSelectors();renderNextAction(gates);
}
function renderQuality(audits,detectors){
  const currentAudit=audits.find(x=>x.is_current&&x.audit_type==='deep_ai')||audits.find(x=>x.is_current),historicalAudit=audits[0];
  const currentDetector=detectors.find(x=>x.is_current),historicalDetector=detectors[0];
  $('auditMetric').textContent=currentAudit?`${currentAudit.audit_type==='deep_ai'?'深审':'结构'} ${currentAudit.score}分 · ${currentAudit.status}`:historicalAudit?'历史报告 · 当前版本失效':'未审计';
  $('auditMetric').className=currentAudit?(currentAudit.status==='通过'?'green-text':'amber-text'):historicalAudit?'amber-text':'muted';
  $('detectorMetric').textContent=currentDetector?`${currentDetector.human_score}% · ${currentDetector.status}`:historicalDetector?'历史报告 · 当前版本失效':'未检测';
  $('detectorMetric').className=currentDetector?(currentDetector.status==='通过'?'green-text':'amber-text'):historicalDetector?'amber-text':'muted';
  renderAuditHistory(audits);
}
function versionOptions(){const versions=currentDetail?.versions||[];return versions.length?versions.map(v=>`<option value="${escapeHtml(v.id)}">${escapeHtml(v.label)} · 第${v.range_start}—${v.range_end}集 · ${escapeHtml(v.sha256.slice(0,10))}</option>`).join(''):'<option value="">尚无正文版本</option>'}
function refreshModuleSelectors(){const html=versionOptions();['auditVersion','detectorVersion','deliveryVersion','gateVersion','continuityVersion'].forEach(id=>{if($(id))$(id).innerHTML=html});if($('gateCode'))$('gateCode').innerHTML=(currentDetail?.gates||[]).map(g=>`<option value="${g.gate_code}" ${g.status==='current'?'selected':''}>${g.gate_code} ${escapeHtml(g.title)} · ${escapeHtml(g.status)}</option>`).join('');renderDetectorReports()}
function renderDetectorReports(){if(!$('detectorReports'))return;const rows=currentDetail?.detectors||[];$('detectorReports').innerHTML=rows.length?rows.slice(0,5).map(x=>`<div class="report-row"><span><strong>人工 ${x.human_score}%</strong><small>${formatTime(x.created_at)} · ${escapeHtml(x.sha256.slice(0,10))}</small></span><span class="source-badge ${x.is_current?(x.status==='通过'?'':'pending'):'pending'}">${x.is_current?escapeHtml(x.status):'历史·已失效'}</span></div>`).join(''):'<div class="empty-state compact">当前项目尚无朱雀报告</div>'}
function renderAuditHistory(rows){if(!$('auditHistory'))return;$('auditHistory').innerHTML=rows.length?rows.slice(0,6).map(x=>`<div class="report-row"><span><strong>${x.audit_type==='deep_ai'?'AI内部深审':'结构审计'} ${x.score}分</strong><small>${formatTime(x.created_at)} · ${escapeHtml(x.sha256.slice(0,10))}</small></span><span class="source-badge ${x.is_current&&x.status==='通过'?'':'pending'}">${x.is_current?escapeHtml(x.status):'历史·已失效'}</span></div>`).join(''):'<div class="empty-state compact">尚无审计记录</div>'}
function renderRuns(runs){
  const statusText={running:'运行中',succeeded:'已完成',failed:'失败'};
  $('runSummary').innerHTML=runs.length?runs.slice(0,2).map(x=>`<div class="run-chip"><strong>${escapeHtml(x.stage||x.task_type)} · ${statusText[x.status]||escapeHtml(x.status)}</strong><small>${Number(x.progress||0)}% · ${formatTime(x.updated_at)}${x.error?' · '+escapeHtml(x.error.slice(0,50)):''}</small></div>`).join(''):'<div class="empty-state compact">还没有模型或深审运行</div>';
  if($('runHistory')){$('runHistory').innerHTML=runs.length?runs.map(x=>`<div class="run-row"><span class="source-badge ${x.status==='succeeded'?'':'pending'}">${statusText[x.status]||escapeHtml(x.status)}</span><div><strong>${escapeHtml(x.task_type)} · ${escapeHtml(x.stage||'')}</strong><small>${escapeHtml(x.instruction||'')}</small><div class="run-bar"><span style="width:${Number(x.progress||0)}%"></span></div>${x.error?`<small class="amber-text">${escapeHtml(x.error)}</small>`:''}${x.status==='failed'?`<button class="small-button retry-run" data-run="${escapeHtml(x.id)}">按原任务重试</button>`:''}</div><small>${formatTime(x.updated_at)}${x.retry_of?'<br>重试任务':''}</small></div>`).join(''):'<div class="empty-state">尚无运行历史</div>';$('runHistory').querySelectorAll('.retry-run').forEach(btn=>btn.addEventListener('click',()=>retryRun(btn.dataset.run)))}
}
function startRunPolling(){let busy=false;const timer=setInterval(async()=>{if(busy||!currentProject())return;busy=true;try{const data=await request(`/api/projects/${encodeURIComponent(currentProject().id)}`);currentDetail.runs=data.detail.runs||[];renderRuns(currentDetail.runs)}catch{}finally{busy=false}},1800);return()=>clearInterval(timer)}
function renderContinuity(rows){if(!$('continuityList'))return;const labels={character:'人物',prop:'道具',timeline:'时间线',permission:'权限/证照',location:'地点'};$('continuityList').innerHTML=rows.length?rows.map(x=>`<div class="continuity-row ${x.is_current?'':'history'}"><span class="source-badge ${x.is_current?'':'pending'}">${labels[x.category]||escapeHtml(x.category)}</span><span><strong>${escapeHtml(x.subject)}</strong><small>${x.source_version_id?(x.is_current?'当前正文提取':'历史正文提取'):'人工条目'}</small></span><span>${escapeHtml(x.state||'待补充')}<small>${escapeHtml(x.notes||'')}</small></span><span>${x.first_episode||'—'}—${x.last_episode||'—'}集</span><span>${escapeHtml(x.status)}</span><button class="delete-entry" data-entry="${escapeHtml(x.id)}" title="删除">×</button></div>`).join(''):'<div class="empty-state">账本为空。可从当前正文提取人物和地点，再人工补充道具、时间、权限和状态变化。</div>';$('continuityList').querySelectorAll('.delete-entry').forEach(btn=>btn.addEventListener('click',()=>deleteContinuity(btn.dataset.entry)))}
function renderNextAction(gates){const g=gates.find(x=>x.status==='current')||gates[gates.length-1];if(!g)return;$('nextActionBtn').dataset.gate=g.gate_code;$('nextActionBtn').previousElementSibling.querySelector('strong').textContent=`处理 ${g.gate_code} ${g.title}`}
function formatBytes(value){const n=Number(value||0);if(n<1024)return`${n} B`;if(n<1048576)return`${(n/1024).toFixed(1)} KB`;return`${(n/1048576).toFixed(1)} MB`}
function renderSources(sources){
  $('sourceList').innerHTML=sources.length?sources.map(s=>`<div class="source-row"><span><strong>${escapeHtml(s.name)}</strong><small>${escapeHtml(s.mime)} · ${escapeHtml(s.sha256.slice(0,12))}</small></span><span>${formatBytes(s.byte_size)}</span><span>${formatTime(s.created_at)}</span><span class="source-badge ${s.extraction_status==='已提取'?'':'pending'}">${escapeHtml(s.extraction_status)}</span></div>`).join(''):'<div class="empty-state">尚无素材。TXT/MD导入后可直接进入后续模型上下文；DOCX/PDF将在解析服务接入后提取正文。</div>';
}
function fileToBase64(file){return new Promise((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve(String(reader.result).split(',')[1]||'');reader.onerror=()=>reject(reader.error||new Error('读取文件失败'));reader.readAsDataURL(file)})}
async function uploadSources(files){const p=currentProject();if(!p||!files.length)return;let ok=0;for(const file of files){try{showToast(`正在导入 ${file.name}…`);const dataBase64=await fileToBase64(file);await request(`/api/projects/${encodeURIComponent(p.id)}/sources`,{method:'POST',body:JSON.stringify({name:file.name,mime:file.type||'application/octet-stream',dataBase64})});ok++}catch(e){showToast(`${file.name}：${e.message}`)}}await selectProject(projects.findIndex(x=>x.selected),false);if(ok)showToast(`已导入 ${ok} 个素材并完成哈希留档`)}
function renderAssets(assets){
  $('assetGrid').innerHTML=Object.entries(assetDefs).map(([type,d])=>{const a=assets[type]||{content:'',version:1};const text=a.content.trim()?a.content.trim().slice(0,56):d.hint;return `<div class="asset-card" data-asset="${type}"><span class="asset-icon">${d.icon}</span><div><strong>${d.title}</strong><p>${escapeHtml(text)}</p></div><span class="asset-status">${a.content.trim()?`v${a.version}`:'待填写'}</span></div>`}).join('');
  $('assetGrid').querySelectorAll('.asset-card').forEach(x=>x.addEventListener('click',()=>openAsset(x.dataset.asset)));
}
function renderVersions(versions){
  const header='<div class="version-row header"><span>版本</span><span>来源</span><span>范围</span><span>状态</span><span></span></div>';
  $('versionTable').innerHTML=header+(versions.length?versions.map(v=>`<div class="version-row"><span><b>${escapeHtml(v.label)}</b><small>${formatTime(v.created_at)} · <span class="hash">${escapeHtml(v.sha256.slice(0,10))}</span></small></span><span>${escapeHtml(v.source)}</span><span>第${v.range_start}—${v.range_end}集</span><span class="green-text">${escapeHtml(v.status)}</span><button class="text-button view-version" data-id="${escapeHtml(v.id)}">查看</button></div>`).join(''):'<div class="empty-state">尚无正文版本。点击“新建版本”保存第一份正文。</div>');
  $('versionTable').querySelectorAll('.view-version').forEach(x=>x.addEventListener('click',()=>viewVersion(x.dataset.id)));
}
function renderGates(gates){
  const current=gates.find(x=>x.status==='current');$('gateProgress').textContent=`${current?.gate_code||'G0'} / G6`;
  $('gateList').innerHTML=gates.map(g=>{const cls=g.status==='passed'?'done':g.status==='current'?'current':'';const mark=g.status==='passed'?'✓':g.gate_code.slice(1);return `<div class="gate ${cls}"><span>${mark}</span><div><strong>${escapeHtml(g.gate_code)} ${escapeHtml(g.title)}</strong><small>${escapeHtml(g.note||g.status)}</small></div></div>`}).join('');
}
function renderEvents(events){
  $('activityLog').innerHTML=events.length?events.slice(0,6).map((e,i)=>`<div class="log-line"><time>${formatTime(e.created_at).slice(-5)}</time><span class="log-dot ${i%3===0?'green':i%3===1?'blue':'amber'}"></span><p>${escapeHtml(e.message)}</p></div>`).join(''):'<div class="empty-state">还没有运行日志</div>';
}
function renderThread(events,project){
  const base=`<article class="thread-card system-card"><div class="card-top"><div class="actor"><span class="actor-icon system">◌</span><div><strong>剧本创作总控台</strong><span class="actor-role">项目工作区</span></div></div><time>当前</time></div><p>「${escapeHtml(project.name)}」已绑定独立资料、正文版本、SOP闸门和活动日志。生成、审校、检测与交付将分别留痕。</p><div class="card-tags"><span class="mini-tag blue-tag">${escapeHtml(project.route)}</span><span class="mini-tag">数据已持久化</span></div></article>`;
  const cards=events.slice(0,8).map(e=>`<article class="thread-card"><div class="card-top"><div class="actor"><span class="actor-icon ai">${e.kind==='prompt'?'你':'✓'}</span><div><strong>${e.kind==='prompt'?'人工任务':'系统记录'}</strong><span class="actor-role">${escapeHtml(e.kind)}</span></div></div><time>${formatTime(e.created_at)}</time></div><p>${escapeHtml(e.message)}</p></article>`).join('');$('threadFeed').innerHTML=base+cards;
}

function toggleModal(id,show){$(id).classList.toggle('hidden',!show)}
function switchTab(name){document.querySelectorAll('.tab').forEach(x=>x.classList.toggle('active',x.dataset.tab===name));['thread','sources','assets','versions'].forEach(x=>$(`${x}Panel`).classList.toggle('hidden',x!==name))}
function openAsset(type){editingAsset=type;const d=assetDefs[type],a=currentDetail.assets[type]||{content:''};$('assetModalTitle').textContent=`编辑${d.title}`;$('assetContent').value=a.content;toggleModal('assetModal',true);$('assetContent').focus()}
async function saveAsset(){const p=currentProject();try{await request(`/api/projects/${encodeURIComponent(p.id)}/assets/${editingAsset}`,{method:'PUT',body:JSON.stringify({content:$('assetContent').value})});toggleModal('assetModal',false);await selectProject(projects.findIndex(x=>x.selected),false);showToast('项目资料已保存并记录新版本')}catch(e){showToast(e.message)}}
function openVersion(){viewingVersion=false;viewingVersionId=null;$('versionLabel').value=`v0.${currentDetail.versions.length+1} 工作稿`;$('versionSource').value='人工编辑';$('versionStart').value=currentDetail.profile?.submission_start||1;$('versionEnd').value=Math.min(currentDetail.profile?.submission_end||10,3);$('versionContent').value='';$('scriptAnalysis').classList.add('hidden');$('analyzeVersionBtn').classList.add('hidden');$('saveVersionBtn').classList.remove('hidden');toggleModal('versionModal',true)}
async function viewVersion(id){try{const {version}=await request(`/api/versions/${encodeURIComponent(id)}/content`);viewingVersion=true;viewingVersionId=id;$('versionLabel').value=version.label;$('versionSource').value=version.source;$('versionStart').value=version.range_start;$('versionEnd').value=version.range_end;$('versionContent').value=version.content;$('scriptAnalysis').classList.add('hidden');$('analyzeVersionBtn').classList.remove('hidden');$('saveVersionBtn').classList.add('hidden');toggleModal('versionModal',true)}catch(e){showToast(e.message)}}
async function analyzeVersion(){if(!viewingVersionId)return;const panel=$('scriptAnalysis');panel.classList.remove('hidden');panel.textContent='正在解析分集、场景、人物与地点…';try{const {analysis}=await request(`/api/versions/${encodeURIComponent(viewingVersionId)}/analysis`);const tree=analysis.episodes.map(ep=>`<details><summary>第${ep.number}集 · ${ep.scenes.length}场</summary>${ep.scenes.map(s=>`<p>${escapeHtml(s.heading)}${s.characters.length?' · '+escapeHtml(s.characters.join('、')):''}</p>`).join('')||'<p>未识别规范场次标题</p>'}</details>`).join('');panel.innerHTML=`<div class="analysis-grid"><div class="analysis-stat"><strong>${analysis.episodeCount}</strong><small>识别集数</small></div><div class="analysis-stat"><strong>${analysis.sceneCount}</strong><small>识别场次</small></div><div class="analysis-stat"><strong>${analysis.characters.length}</strong><small>登场人物</small></div></div><p><strong>高频人物：</strong>${escapeHtml(analysis.characters.slice(0,8).map(x=>`${x.name}(${x.scenes})`).join('、')||'未识别')}</p><p><strong>主要地点：</strong>${escapeHtml(analysis.locations.slice(0,8).map(x=>`${x.name}(${x.scenes})`).join('、')||'未识别')}</p><div class="scene-tree">${tree}</div>`}catch(e){panel.textContent=e.message}}
async function saveVersion(){const p=currentProject();const body={label:$('versionLabel').value.trim(),source:$('versionSource').value,rangeStart:Number($('versionStart').value),rangeEnd:Number($('versionEnd').value),content:$('versionContent').value};try{await request(`/api/projects/${encodeURIComponent(p.id)}/versions`,{method:'POST',body:JSON.stringify(body)});toggleModal('versionModal',false);await selectProject(projects.findIndex(x=>x.selected),false);showToast('正文已保存为新版本，旧版本未被覆盖')}catch(e){showToast(e.message)}}

function openSettings(){const c=getModelConfig();$('llmBaseUrl').value=c.baseUrl||'';$('llmModel').value=c.model||'';$('llmWire').value=c.wireApi||'responses';$('llmReasoning').value=c.reasoning||'high';$('llmKey').value=c.apiKey||'';$('modelTestResult').textContent='尚未测试接口';toggleModal('settingsModal',true)}
function readSettings(){return{baseUrl:$('llmBaseUrl').value.trim(),apiKey:$('llmKey').value.trim(),model:$('llmModel').value.trim(),wireApi:$('llmWire').value,reasoning:$('llmReasoning').value}}
async function testModel(){const btn=$('testModelBtn');btn.disabled=true;$('modelTestResult').textContent='正在连接接口…';try{const data=await request('/api/model/test',{method:'POST',body:JSON.stringify(readSettings())});$('modelTestResult').textContent=`测试成功：${data.output}`;}catch(e){$('modelTestResult').textContent=`测试失败：${e.message}`}finally{btn.disabled=false}}
async function runModelTask(){const instruction=$('promptInput').value.trim(),taskType=$('taskType').value,p=currentProject(),config=getModelConfig();if(!instruction){showToast('请先输入任务内容');return}if(!config.apiKey){openSettings();showToast('请先配置并测试模型接口');return}const btn=$('sendBtn');btn.disabled=true;btn.innerHTML='运行中…';const stopPolling=startRunPolling();try{const profile=currentDetail.profile||{};const data=await request(`/api/projects/${encodeURIComponent(p.id)}/model/run`,{method:'POST',body:JSON.stringify({instruction,taskType,config,rangeStart:profile.submission_start||1,rangeEnd:profile.submission_end||10,label:`模型稿 ${new Date().toLocaleString('zh-CN')}`})});$('promptInput').value='';await selectProject(projects.findIndex(x=>x.selected),false);if(taskType==='reply')showToast(`模型已回答：${data.output.slice(0,80)}`);else{showToast('模型任务完成并已保存新版本');switchTab(taskType==='draft'?'versions':'assets')}}catch(e){showToast(e.message)}finally{stopPolling();btn.disabled=false;btn.innerHTML='运行 <span>↗</span>'}}
async function runAudit(){const versionId=$('auditVersion').value;if(!versionId){showToast('请先保存正文版本');return}const btn=$('runAuditBtn');btn.disabled=true;$('auditResult').textContent='正在检查正文结构…';try{const {report}=await request(`/api/projects/${encodeURIComponent(currentProject().id)}/audits`,{method:'POST',body:JSON.stringify({versionId})});const list=report.findings.length?report.findings.map(x=>`<li class="${x.level}">${escapeHtml(x.message)}</li>`).join(''):'<li class="pass">未发现结构问题</li>';$('auditResult').innerHTML=`<strong>${report.score}分 · ${escapeHtml(report.status)}</strong><p>${escapeHtml(report.summary)}</p><ul>${list}</ul>`;await selectProject(projects.findIndex(x=>x.selected),false)}catch(e){$('auditResult').textContent=e.message}finally{btn.disabled=false}}
async function runDeepAudit(){const versionId=$('auditVersion').value,config=getModelConfig();if(!versionId){showToast('请先保存正文版本');return}if(!config.apiKey){openSettings();showToast('AI深审需要先配置模型接口');return}const btn=$('runDeepAuditBtn');btn.disabled=true;$('auditResult').textContent='正在执行八维评分、五张反查表和硬错误定位…';const stopPolling=startRunPolling();try{const {report}=await request(`/api/projects/${encodeURIComponent(currentProject().id)}/audits/deep`,{method:'POST',body:JSON.stringify({versionId,config})});const labels={premise:'设定与题眼',causality:'因果',characters:'人物',dialogue:'对白',pacing:'节奏',emotion:'情绪',performance:'可表演性',continuity:'连续性'},reverseLabels={prop_chain:'道具链',permission_license:'权限/证照',adjacent_transition:'相邻转场',presence_speaker:'在场/说话人',packaging_identity:'包装身份'};const dims=Object.entries(report.dimensions||{}).map(([k,v])=>`<div class="dimension-row"><span>${labels[k]||k}</span><strong>${v.score}/${v.max}</strong></div>`).join('');const reverse=Object.entries(report.reverseChecks||{}).map(([k,v])=>`<div class="dimension-row"><span>${reverseLabels[k]||k}</span><strong class="${v.status==='pass'||v.status==='not_applicable'?'green-text':'amber-text'}">${escapeHtml(v.status)}</strong></div>`).join('');const errors=(report.findings||[]).length?(report.findings||[]).map(x=>`<li class="error">${escapeHtml(x.message||'')}</li>`).join(''):'<li class="pass">模型未报告硬错误，五张反查表均已交付</li>';$('auditResult').innerHTML=`<strong>AI内部深审 ${report.score}分 · ${escapeHtml(report.status)}</strong><p>${escapeHtml(report.summary)}</p><h4>八维评分</h4><div class="audit-dimensions">${dims}</div><h4>五张反查表</h4><div class="audit-dimensions">${reverse}</div><ul>${errors}</ul><p class="muted">该分数用于内部筛查，仍需人工逐项确认后批准G4。</p>`;await selectProject(projects.findIndex(x=>x.selected),false)}catch(e){$('auditResult').textContent=e.message}finally{stopPolling();btn.disabled=false}}
async function decideGate(decision){const gateCode=$('gateCode').value,versionId=$('gateVersion').value,note=$('gateNote').value.trim();try{await request(`/api/projects/${encodeURIComponent(currentProject().id)}/gates/${encodeURIComponent(gateCode)}`,{method:'POST',body:JSON.stringify({decision,versionId,note})});toggleModal('gateModal',false);await selectProject(projects.findIndex(x=>x.selected),false);showToast(decision==='approve'?'阶段已由人工批准':'阶段已驳回并记录原因')}catch(e){showToast(e.message)}}
async function retryRun(runId){const run=(currentDetail?.runs||[]).find(x=>x.id===runId),config=getModelConfig();if(!run)return;if(!config.apiKey){openSettings();showToast('重试需要当前浏览器重新提供有效模型配置');return}const stopPolling=startRunPolling();try{if(run.task_type==='deep_audit'){if(!run.version_id)throw new Error('原深审未绑定正文版本，无法重试');await request(`/api/projects/${encodeURIComponent(currentProject().id)}/audits/deep`,{method:'POST',body:JSON.stringify({versionId:run.version_id,retryOf:run.id,config})})}else{const profile=currentDetail.profile||{};await request(`/api/projects/${encodeURIComponent(currentProject().id)}/model/run`,{method:'POST',body:JSON.stringify({instruction:run.instruction,taskType:run.task_type,retryOf:run.id,config,rangeStart:profile.submission_start||1,rangeEnd:profile.submission_end||10,label:`重试稿 ${new Date().toLocaleString('zh-CN')}`})})}await selectProject(projects.findIndex(x=>x.selected),false);showToast('重试完成，新运行记录已关联原任务')}catch(e){showToast(e.message)}finally{stopPolling()}}
async function addContinuity(){const p=currentProject(),body={category:$('continuityCategory').value,subject:$('continuitySubject').value.trim(),state:$('continuityState').value.trim(),firstEpisode:$('continuityFirst').value,lastEpisode:$('continuityLast').value,notes:$('continuityNotes').value.trim()};if(!body.subject){showToast('请填写连续性主体');return}try{await request(`/api/projects/${encodeURIComponent(p.id)}/continuity`,{method:'POST',body:JSON.stringify(body)});['continuitySubject','continuityState','continuityFirst','continuityLast','continuityNotes'].forEach(id=>$(id).value='');await selectProject(projects.findIndex(x=>x.selected),false);showToast('连续性条目已保存')}catch(e){showToast(e.message)}}
async function extractContinuity(){const versionId=$('continuityVersion').value;if(!versionId){showToast('请先选择正文版本');return}const btn=$('extractContinuityBtn');btn.disabled=true;try{const data=await request(`/api/projects/${encodeURIComponent(currentProject().id)}/continuity/extract`,{method:'POST',body:JSON.stringify({versionId})});await selectProject(projects.findIndex(x=>x.selected),false);showToast(`已从正文提取${data.count}条人物/地点索引`)}catch(e){showToast(e.message)}finally{btn.disabled=false}}
async function deleteContinuity(id){if(!window.confirm('确认删除这条连续性记录？该操作不会删除正文版本。'))return;try{await request(`/api/continuity/${encodeURIComponent(id)}`,{method:'DELETE'});await selectProject(projects.findIndex(x=>x.selected),false);showToast('连续性条目已删除')}catch(e){showToast(e.message)}}
async function saveDetector(){const versionId=$('detectorVersion').value,file=$('detectorFile').files[0];if(!versionId||!file){showToast('请选择正文版本并上传朱雀报告截图');return}const btn=$('saveDetectorBtn');btn.disabled=true;try{const dataBase64=await fileToBase64(file);await request(`/api/projects/${encodeURIComponent(currentProject().id)}/detectors`,{method:'POST',body:JSON.stringify({versionId,humanScore:Number($('humanScore').value),suspectedScore:Number($('suspectedScore').value||0),aiScore:Number($('aiScore').value||0),reportName:file.name,mime:file.type,dataBase64})});await selectProject(projects.findIndex(x=>x.selected),false);showToast('真实检测指标、截图和正文哈希已绑定保存')}catch(e){showToast(e.message)}finally{btn.disabled=false}}
function downloadExport(format){const versionId=$('deliveryVersion').value;if(!versionId){showToast('请先选择正文版本');return}const url=`/api/projects/${encodeURIComponent(currentProject().id)}/export?format=${format}&versionId=${encodeURIComponent(versionId)}`;const a=document.createElement('a');a.href=url;a.click();showToast(format==='json'?'正在下载项目快照':'正在下载TXT合并稿')}

$('newProjectBtn').addEventListener('click',()=>toggleModal('projectModal',true));$('closeModal').addEventListener('click',()=>toggleModal('projectModal',false));$('cancelModal').addEventListener('click',()=>toggleModal('projectModal',false));
$('createProject').addEventListener('click',async()=>{const body={name:$('newProjectName').value.trim(),route:$('newProjectRoute').value,region:$('newProjectRegion').value,medium:$('newProjectMedium').value,totalEpisodes:Number($('newTotalEpisodes').value),submissionStart:1,submissionEnd:Number($('newSubmissionEnd').value),durationMin:Number($('newDurationMin').value),durationMax:Number($('newDurationMax').value),qualityThreshold:Number($('newQualityThreshold').value),zhuqueThreshold:Number($('newZhuqueThreshold').value),openingTemplate:$('newOpeningTemplate').value,genre:$('newGenre').value.trim()||'待设定'};try{const {project}=await request('/api/projects',{method:'POST',body:JSON.stringify(body)});projects.forEach(x=>x.selected=false);projects.unshift({...project,selected:true});renderProjects();toggleModal('projectModal',false);$('newProjectName').value='';await selectProject(0,false);showToast('项目已创建并建立独立工作空间')}catch(e){showToast(e.message)}});
$('closeAssetModal').addEventListener('click',()=>toggleModal('assetModal',false));$('cancelAssetModal').addEventListener('click',()=>toggleModal('assetModal',false));$('saveAssetBtn').addEventListener('click',saveAsset);
$('newVersionBtn').addEventListener('click',openVersion);$('closeVersionModal').addEventListener('click',()=>toggleModal('versionModal',false));$('cancelVersionModal').addEventListener('click',()=>toggleModal('versionModal',false));$('saveVersionBtn').addEventListener('click',saveVersion);
$('analyzeVersionBtn').addEventListener('click',analyzeVersion);
['projectModal','assetModal','versionModal'].forEach(id=>$(id).addEventListener('click',e=>{if(e.target.id===id)toggleModal(id,false)}));
document.querySelectorAll('.tab').forEach(tab=>tab.addEventListener('click',()=>switchTab(tab.dataset.tab)));
$('uploadSourceBtn').addEventListener('click',()=>$('sourceFileInput').click());$('sourceFileInput').addEventListener('change',async e=>{await uploadSources([...e.target.files]);e.target.value=''});
document.querySelectorAll('.nav-item,.settings-link').forEach(item=>item.addEventListener('click',()=>{document.querySelectorAll('.nav-item').forEach(x=>x.classList.remove('active'));if(item.classList.contains('nav-item'))item.classList.add('active');const view=item.dataset.view;if(view==='overview'||view==='tasks'){switchTab('thread');if(view==='tasks')$('promptInput').focus()}else if(view==='drafts')switchTab('versions');else if(view==='audit')toggleModal('auditModal',true);else if(view==='continuity')toggleModal('continuityModal',true);else if(view==='detector')toggleModal('detectorModal',true);else if(view==='delivery')toggleModal('deliveryModal',true);else if(view==='settings')openSettings()}));
$('sendBtn').addEventListener('click',runModelTask);
$('promptInput').addEventListener('keydown',e=>{if(e.ctrlKey&&e.key==='Enter')$('sendBtn').click()});
$('runTaskBtn').addEventListener('click',()=>{$('promptInput').focus();showToast('请在下方选择任务类型并输入指令')});
$('composerAttachment').addEventListener('click',()=>{$('sourceFileInput').click();switchTab('sources')});
$('openSettingsBtn').addEventListener('click',openSettings);$('closeSettingsModal').addEventListener('click',()=>toggleModal('settingsModal',false));$('saveSettingsBtn').addEventListener('click',()=>{const c=readSettings();setModelConfig(c);toggleModal('settingsModal',false);showToast('模型设置仅保存于当前浏览器会话')});$('testModelBtn').addEventListener('click',testModel);
$('closeAuditModal').addEventListener('click',()=>toggleModal('auditModal',false));$('runAuditBtn').addEventListener('click',runAudit);$('runDeepAuditBtn').addEventListener('click',runDeepAudit);$('qualityDetailBtn').addEventListener('click',()=>toggleModal('auditModal',true));
$('closeDetectorModal').addEventListener('click',()=>toggleModal('detectorModal',false));$('saveDetectorBtn').addEventListener('click',saveDetector);
$('closeDeliveryModal').addEventListener('click',()=>toggleModal('deliveryModal',false));$('downloadTxtBtn').addEventListener('click',()=>downloadExport('txt'));$('downloadJsonBtn').addEventListener('click',()=>downloadExport('json'));
$('reviewBtn')?.addEventListener('click',()=>showToast('请在正文版本中打开校准稿'));$('nextActionBtn').addEventListener('click',()=>toggleModal('gateModal',true));
$('runHistoryBtn').addEventListener('click',()=>toggleModal('runModal',true));$('allLogsBtn').addEventListener('click',()=>switchTab('thread'));$('closeRunModal').addEventListener('click',()=>toggleModal('runModal',false));
$('closeGateModal').addEventListener('click',()=>toggleModal('gateModal',false));$('approveGateBtn').addEventListener('click',()=>decideGate('approve'));$('rejectGateBtn').addEventListener('click',()=>decideGate('reject'));
$('closeContinuityModal').addEventListener('click',()=>toggleModal('continuityModal',false));$('addContinuityBtn').addEventListener('click',addContinuity);$('extractContinuityBtn').addEventListener('click',extractContinuity);
['settingsModal','auditModal','detectorModal','deliveryModal','runModal','gateModal','continuityModal'].forEach(id=>$(id).addEventListener('click',e=>{if(e.target.id===id)toggleModal(id,false)}));renderModelName();loadHealth();loadProjects();
